# AGENTS.MD

READ ~/Projects/agent-scripts/AGENTS.MD BEFORE ANYTHING (skip if files missing).

Commander notes: keep this submodule in sync with Peekaboo; update here first, then bump the pointer in the parent repo.
